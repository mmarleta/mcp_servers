# Backend Orchestrator MCP Server Requirements
# Production-grade dependencies for Optimus development tools

# MCP Framework
mcp>=1.0.0

# HTTP Client (for AI Engine integration testing)
httpx>=0.27.0
aiofiles>=24.1.0

# Redis (for cache coherence and rate limiting analysis)
redis[hiredis]>=5.0.0

# JSON and Data Processing
pydantic>=2.5.0
python-dateutil>=2.8.2

# Async Support
asyncio-throttle>=1.0.2
aioredis>=2.0.1

# Development and Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-mock>=3.12.0

# Code Quality (for router validation)
ruff>=0.1.0
mypy>=1.8.0

# Logging and Monitoring
structlog>=23.2.0
prometheus-client>=0.19.0

# Optional: Performance monitoring
psutil>=5.9.0
memory-profiler>=0.60.0